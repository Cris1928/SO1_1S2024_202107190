version: '3.1'

services:
  mongo:
    image: mongo
    container_name: 'MongoDB'
    restart: always
    volumes:
      - Mongo_DB:/var/lib/mongo      
    ports:
      - "27017:27017"
volumes:
  Mongo_DB: